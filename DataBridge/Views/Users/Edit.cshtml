@* FILE: Views/Users/Edit.cshtml *@
@model DataBridge.Models.ViewModels.Users.UserEditViewModel
@{
    ViewData["Title"] = $"Edit — {Model.Username}";
}

<div class="page-header">
    <div>
        <div class="page-title">Edit User</div>
        <div class="page-subtitle">Update account details for <strong>@Model.Username</strong></div>
    </div>
    <div class="page-actions">
        <a href="/Users/ResetPassword/@Model.Id" class="btn btn-secondary">
            <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
            Reset Password
        </a>
        <a href="/Users" class="btn btn-secondary">
            <svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"/></svg>
            Back
        </a>
    </div>
</div>

<div style="max-width:560px;">
    <div class="card">
        <div class="card-header">
            <span class="card-title">Account Details</span>
            <span style="font-family:var(--mono);font-size:11px;color:var(--text-muted);">
                Created @Model.CreatedAt.ToString("dd MMM yyyy")
            </span>
        </div>
        <div class="card-body">

            @if (!ViewData.ModelState.IsValid && ViewData.ModelState.Values.Any(v => v.Errors.Count > 0))
            {
                <div class="alert alert-danger">
                    <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
                    <div>
                        @foreach (var err in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <div>@err.ErrorMessage</div>
                        }
                    </div>
                </div>
            }

            <form method="post" action="/Users/Edit">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="Username" />
                <input type="hidden" asp-for="CreatedAt" />

                <!-- Username (readonly) -->
                <div class="form-group">
                    <label class="form-label">Username</label>
                    <input type="text" value="@Model.Username" class="form-control"
                           disabled style="background:var(--bg-surface-2);color:var(--text-muted);font-family:var(--mono);" />
                    <div class="form-hint">Username cannot be changed</div>
                </div>

                <div class="form-group">
                    <label class="form-label">Full Name <span style="color:var(--danger)">*</span></label>
                    <input asp-for="FullName" class="form-control @(ViewData.ModelState["FullName"]?.Errors.Count > 0 ? "is-invalid" : "")"
                           placeholder="e.g. John Doe" />
                </div>

                <div class="form-group">
                    <label class="form-label">Email <span style="color:var(--danger)">*</span></label>
                    <input asp-for="Email" type="email" class="form-control @(ViewData.ModelState["Email"]?.Errors.Count > 0 ? "is-invalid" : "")"
                           placeholder="e.g. john@company.com" />
                </div>

                <div class="form-group">
                    <label class="form-label">Role <span style="color:var(--danger)">*</span></label>
                    <select asp-for="Role" class="form-control">
                        <option value="1">Admin</option>
                        <option value="2">Viewer</option>
                    </select>
                </div>

                <div class="section-divider"></div>

                <div class="form-group" style="display:flex;align-items:center;gap:10px;margin-bottom:20px;">
                    <input asp-for="IsActive" type="checkbox" id="isActive"
                           style="width:16px;height:16px;accent-color:var(--accent);cursor:pointer;" />
                    <label for="isActive" style="font-size:13px;cursor:pointer;color:var(--text-secondary);">
                        Account is active
                    </label>
                </div>

                <div style="display:flex;gap:8px;">
                    <button type="submit" class="btn btn-primary">
                        <svg viewBox="0 0 24 24"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
                        Save Changes
                    </button>
                    <a href="/Users" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>
